We tried to separate the page size from the page table size. These are the same on x86 and on ARM they have been adjusted to be the same, but on TilePro they are very different. It didn't work for some reason, but here the patch of that try. barrelfish_tilepro_try_to_separate_page_size_and_pagetable_size.patch, based on Revision 1306:9edab704c48c (no-op implementation of bulk transfer)
